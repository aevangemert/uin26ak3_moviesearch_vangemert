import { useParams } from "react-router-dom"

//prøv å lage en useState som skal vise 1 film istedet for useParams, definere i app??

export default function Movie({setSelectedMovie}){
   return(<h1>Filmer</h1>)
}
